name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install deno and ru test
      run: |
        curl -fsSL https://deno.land/x/install/install.sh | sh
        ./deno/deno run test.ts
